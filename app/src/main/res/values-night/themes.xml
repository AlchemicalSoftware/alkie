<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Alkie" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="android:backgroundTint">@color/dark_bg</item>
        <item name="android:buttonStyle">@color/dark_button</item>
        <item name="android:textAppearanceButton">@color/dark_text</item>
        <item name="android:textColor">@color/dark_text</item>
        <item name="android:editTextColor">@color/dark_text</item>
        <item name="android:editTextStyle">@color/dark_text</item>
        <item name="android:colorControlActivated">@color/dark_text</item>
        <item name="android:colorControlNormal">@color/dark_text</item>
        <item name="android:textColorHint">@color/dark_hint</item>
        <item name="colorAccent">@color/dark_text</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>