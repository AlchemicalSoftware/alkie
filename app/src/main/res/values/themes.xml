<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Alkie" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="colorPrimary">@color/light_primary</item>
        <item name="android:buttonStyle">@color/light_button</item>
        <item name="android:textAppearanceButton">@color/light_bg</item>
        <item name="android:textColor">@color/light_text</item>
        <item name="android:editTextColor">@color/light_text</item>
        <item name="android:editTextStyle">@color/light_text</item>
        <item name="android:colorControlActivated">@color/light_text</item>
        <item name="android:colorControlNormal">@color/light_text</item>
        <item name="android:textColorHint">@color/light_hint</item>
        <item name="colorAccent">@color/light_text</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Alkie" parent="Base.Theme.Alkie" />
</resources>